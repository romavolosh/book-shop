<div class="welcome-section">
    <h1>Ласкаво просимо до книжкового магазину</h1>
    <p class="welcome-text">Знайдіть свою наступну улюблену книгу в нашому магазині</p>
</div>

<div class="posts-section">
    <% if (posts && posts.length > 0) { %>
        <div class="posts-grid">
            <% posts.forEach(post => { %>
                <div class="post-card">
                    <div class="post-image">
                        <img src="<%= post.imageUrl %>" alt="<%= post.title %>">
                    </div>
                    <div class="post-content">
                        <h2><%= post.title %></h2>
                        <p><%= post.content %></p>
                        <small class="post-date"><%= new Date(post.createdAt).toLocaleDateString('uk-UA') %></small>
                    </div>
                </div>
            <% }); %>
        </div>
    <% } else { %>
        <p class="no-posts">Наразі немає доступних постів</p>
    <% } %>
</div>